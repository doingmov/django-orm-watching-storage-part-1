# Пульт охраны (Django)

Это учебный проект на Django, который позволяет просматривать пропуска сотрудников и историю их посещений.  
Проект подключается к базе данных, в которой хранятся пропуски и посещения.

## 🚀 Как запустить

### 1. Клонируйте репозиторий
```
git clone https://github.com/doingmov/django-orm-watching-storage-part-1.git
```
```
cd django-orm-watching-storage-part-1
```

### 2. Создайте виртуальное окружение
```
python -m venv venv
```
```
source venv/bin/activate # Linux / macOS
```
```
venv\Scripts\activate # Windows
```

### 3. Установите зависимости
```
pip install -r requirements.txt
```

### 4. Создайте файл `.env`
В корне проекта создайте файл с расширением .env и запишите в него данные:
```
SECRET_KEY=ваш_секретный_ключ
DB_HOST=localhost
DB_PORT=5432
DB_NAME=passcard
DB_USER=postgres
DB_PASSWORD=пароль_от_бд
```

> Файл `.env` должен находиться рядом с `settings.py`.

## ▶️ Запуск скрипта

Запустить программу можно так:
```
python main.py
```

Скрипт подключится к базе данных и выведет:

- количество всех пропусков  
- количество активных пропусков  
- дополнительные данные, которые вы запрограммировали

Пример вывода:

```
Всего пропусков: 100
Активных пропусков: 89
```

## 🛠 Используемые технологии:

- Python 3  
- Django  
- PostgreSQL  
- python-dotenv

## 📄 Назначение проекта:

Код написан в образовательных целях на онлайн-курсе для веб-разработчиков [dvmn.org](https://dvmn.org).
